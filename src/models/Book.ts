export type Book = {
    pageNumber: number
    categories: string[]
    _id: string
    bookId: string
    isbn: number
    title: string
    publishedYear: number
    coverImage: string
    quantity: number
    rating: number
    summary: string
  }
  