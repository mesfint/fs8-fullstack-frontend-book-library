import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  grid: {
    display: 'flex',
    flexWrap: 'wrap',
  },
  spinner: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    margin: '0 auto',
  },
})
